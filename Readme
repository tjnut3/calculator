!! วิธีการใช้งาน git hub เบื้องต้น !!

เข้าไปเชื่อมต่อก่อนโดยใช้   # () ไม่ต้องใส่วงเล็บ เขียนให้เห็นเฉยๆ
git remote (ลิ้ง git hub)

การจัดการไฟล์ต่างๆ
git init
git add (ชื่อไฟล์)   # . ใช้แทนชื่อไฟล์ถ้าแยากอัพโหลดทั้งหมด
git commit -m "ใส่คอมเม้นข้อความ" # การ comfirm อัพโหลด
git push # อัพโหลดไฟล์ขึ้น GitHub

git pull # ดาวโหลดไฟล์จาก GitHub ลงมา
